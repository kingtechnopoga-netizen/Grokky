{
  "name": "grokky-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny backend proxy that lets the Grokky frontend call Anthropic Claude without exposing the API key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  }
}
